# Docker Image Push & Registry Integration

The pipeline securely publishes Docker images to Docker Hub.

## Key Features
- CI-based image push (no local push)
- Secure authentication using access tokens
- GitHub Secrets for credential management

## Image Details
- Registry: Docker Hub
- Repository: adikannan/git-to-docker-app
- Tag: latest

This ensures a traceable and auditable image publishing process.
